# [0018] 文档与 .gitignore 同步：补充 profile 自选流程说明，清理旧架构残留

## 如何验证

1. 目审 `git diff` 中 README.md、docs/OPERATIONS.md、.gitignore 的改动是否符合预期。
2. 确认 `.gitignore` 删除的条目不影响现有文件：
   ```bash
   ls -d hosts username 2>/dev/null || echo OK   # 应输出 OK
   git status --short                             # 无被误删条目影响的新增文件
   ```
3. 确认文档锚点链接未受影响（本次仅改正文，未动标题）。

---

## 2026/08/19 文档与 .gitignore 同步

## What

- `README.md`「第二步：配置初始化与应用」步骤 3 补充 profile 自选说明（清单解析自 `home/packages.nix`，默认全部不启用），并新增重复运行行为说明（重选询问 / 缺块补齐）。
- `docs/OPERATIONS.md` §4 步骤列表同步 [0013] 引入的 profile 自选流程，步骤 1 合并依赖检查与 Flakes 开启，并补充重复运行行为说明。
- `docs/OPERATIONS.md` §7.1 补充 [0016] 单一数据源说明（setup 脚本交互清单从 `profileMeta` 自动解析）；示例描述同步为去掉「启用」前缀的新文案。
- `.gitignore` 删除旧架构残留条目 `username` 与 `hosts/*.nix`（host 配置已外置到 `~/.config/limac/host.nix`，仓库内无对应文件）。
- 根目录 `flake.lock.backup.*` 遗留文件已在 [0017] 清理，本次核实无残留。

## Why

- 0013（统一自选 profile）与 0016（profile 清单单一数据源）落地后，README 与 OPERATIONS.md 的初始化步骤描述未同步，按旧文档操作会对交互流程产生误解。
- `.gitignore` 中的 `username`、`hosts/*.nix` 是 host 配置外置前的遗留规则，对应文件/目录均不存在，徒增理解成本。

## 改动说明

| 区域 | 改动 |
|---|---|
| `README.md` 第二步 | 步骤 3 补充 profile 自选说明；新增重复运行行为提示 |
| `docs/OPERATIONS.md` §4 | 步骤列表同步 profile 自选流程；新增重复运行行为提示 |
| `docs/OPERATIONS.md` §7.1 | 补充「setup 脚本从 profileMeta 解析」说明；示例文案同步 |
| `.gitignore` | 删除 `username`、`hosts/*.nix` 两个残留块 |

## 验收标准

- README/OPERATIONS 初始化步骤与 `bin/home-manager-setup` 实际行为一致（含 profile 自选与重复运行行为）
- §7.1 明确 `profileMeta` 为唯一数据源，示例文案与 `home/packages.nix` 一致
- `.gitignore` 仅保留有效规则；删除条目不影响任何现有文件

## 相关文件

```
README.md
docs/OPERATIONS.md
.gitignore
devel/0018.md
```
