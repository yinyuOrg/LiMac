# [0004] LiMac 启动 Logo 替换与效果预览

## 如何验证

1. 运行初始化脚本，确认启动 Logo 正确显示：
   ```bash
   bin/home-manager-setup
   ```
2. 确认 README.md 中"效果预览"章节可见且 GIF 正常加载

---

## 2026/07/26 LiMac 启动 Logo 替换与效果预览

## What

- 将 `bin/home-manager-setup` 启动时显示的多行 ASCII art logo 替换为 ██╗ 块字符风格的 "LiMac" banner
- 在 README.md 新增"🎬 效果预览"章节，居中展示 `docs/show.gif` 演示动画
- 在目录中加入"效果预览"跳转链接

---

## 改动说明

| 区域 | 改动 |
|---|---|
| `bin/home-manager-setup` | Logo 从复杂多行 ASCII art 替换为 ██╗ 块字符风格 Banner |
| `README.md` | 新增 `## 🎬 效果预览` 章节，居中插入 `docs/show.gif`；目录新增对应链接 |

---

## 验收标准

- `bin/home-manager-setup` 运行时显示新的 ██╗ 风格 LiMac Logo
- README.md 中 GIF 可正常显示
- 目录链接可正常跳转

---

## 相关文件

```
bin/home-manager-setup
README.md
docs/show.gif
```
